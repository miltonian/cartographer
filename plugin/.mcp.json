{
  "mcpServers": {
    "cartographer": {
      "command": "sh",
      "args": ["${CLAUDE_PLUGIN_ROOT}/start.sh"],
      "env": {
        "CARTOGRAPHER_DATA": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
