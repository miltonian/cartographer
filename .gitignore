node_modules/
# Ignore all compiled output except dist/ui, which is tracked — it ships with the
# plugin so the browser UI works on marketplace install.
dist/*
!dist/ui/
.cartographer/
scripts/verify/out/
.playwright-mcp/
*.tsbuildinfo
.claude/worktrees/
